#Created on Fri Nov 06 16:16:08 2015

#@author: ANISH

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
from sklearn import svm

data = pd.read_excel("training.xlsx")
data.head()

#print data.shape
#print data1.shape
feature_cols = ['word_count', 'sentence_count', 'spell_error', 'noun_count', 'adjective_count', 'verb_count']
X = np.array(data[feature_cols])
#print(X)
feature_cols1=['total_score']
y=np.array(data[feature_cols1])
#clf = svm.SVC()
clf = svm.SVC()
clf.fit(X,y)
#This is just trying out predict not really implemented on our testing data
#print(clf.predict(X[1]))
